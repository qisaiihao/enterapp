# 小程序审核优化 - 登录状态检测修复

## 问题描述

用户反馈：缓存了登录状态，但点击个人主页时显示未登录，然后跳转到登录页面后，登录页面检测到缓存的登录状态就自动跳过了登录，导致个人主页始终判定为未登录。

## 根本原因

`isLoggedIn` 标记只在新用户登录时设置，但在以下关键场景中没有设置：

1. **App.vue 缓存验证成功时** - 应用启动时检测到有效缓存，但没有设置 `isLoggedIn`
2. **登录页面自动跳转时** - `tryAutoRedirect` 检测到缓存，但没有设置 `isLoggedIn`
3. **注册成功时** - 注册完成后没有设置 `isLoggedIn`

## 修复方案

### 1. App.vue - 缓存验证成功时设置登录状态

**文件**: `App.vue`
**方法**: `loginAndCheckUser`

```javascript
if (verifyRes.result && verifyRes.result.success && verifyRes.result.userInfo) {
    const latestUserInfo = verifyRes.result.userInfo;
    const appInstance = getApp();
    if (appInstance) {
        appInstance.globalData.userInfo = latestUserInfo;
        appInstance.globalData.openid = latestUserInfo._openid;
        appInstance.globalData._loginProcessCompleted = true;
        // 【关键修复】设置登录状态标记
        appInstance.globalData.isLoggedIn = true;
    }
    // ...
}
```

### 2. 登录页面 - 登录成功时设置登录状态

**文件**: `pages/login/login.vue`
**方法**: `handleLoginResult`

```javascript
if (result.success) {
    const app = getApp();
    app.globalData.userInfo = result.userInfo;
    app.globalData.openid = result.openid;
    app.globalData._loginProcessCompleted = true;
    // 【关键修复】设置登录状态标记
    app.globalData.isLoggedIn = true;
    // ...
}
```

### 3. 登录页面 - 自动跳转时设置登录状态

**文件**: `pages/login/login.vue`
**方法**: `tryAutoRedirect`

```javascript
tryAutoRedirect: function () {
    try {
        const cachedUserInfo = uni.getStorageSync('userInfo');
        const cachedOpenId = uni.getStorageSync('userOpenId');
        if (cachedUserInfo && (cachedUserInfo._openid || cachedOpenId)) {
            const app = getApp();
            app.globalData.userInfo = cachedUserInfo;
            app.globalData.openid = cachedUserInfo._openid || cachedOpenId;
            app.globalData._loginProcessCompleted = true;
            // 【关键修复】设置登录状态标记
            app.globalData.isLoggedIn = true;
            uni.switchTab({ url: '/pages/poem-square/poem-square' });
        }
    } catch (e) {
        console.log('⚠️ [登录页面] 自动跳转检查失败(忽略)：', e);
    }
}
```

### 4. 注册页面 - 注册成功时设置登录状态

**文件**: `pages/register/register.vue`

```javascript
if (registerRes.result && registerRes.result.success) {
    const app = getApp();
    app.globalData.userInfo = userInfo;
    app.globalData.openid = returnedOpenid;
    app.globalData._loginProcessCompleted = true;
    // 【关键修复】设置登录状态标记
    app.globalData.isLoggedIn = true;
    // ...
}
```

## 登录状态设置时机总结

`app.globalData.isLoggedIn` 应该在以下所有场景中设置为 `true`：

1. ✅ **App.vue - 缓存验证成功** (启动时检测到有效缓存)
2. ✅ **App.vue - 新用户登录成功** (云端登录成功)
3. ✅ **登录页面 - 登录成功** (handleLoginResult)
4. ✅ **登录页面 - 自动跳转** (tryAutoRedirect检测到缓存)
5. ✅ **注册页面 - 注册成功**

## 测试验证

### 测试场景1：首次登录
1. 清除缓存
2. 打开小程序
3. 浏览内容（无需登录）
4. 点击"我"，提示登录
5. 完成登录
6. 验证：`app.globalData.isLoggedIn === true`
7. 可以正常访问个人主页

### 测试场景2：有缓存启动
1. 已登录状态下关闭小程序
2. 重新打开小程序
3. 验证：`app.globalData.isLoggedIn === true`
4. 点击"我"，直接进入个人主页（不提示登录）

### 测试场景3：登录页面自动跳转
1. 已登录状态
2. 手动进入登录页面
3. 验证：自动跳转到首页
4. 验证：`app.globalData.isLoggedIn === true`

### 测试场景4：注册新用户
1. 清除缓存
2. 注册新用户
3. 验证：`app.globalData.isLoggedIn === true`
4. 可以正常访问个人主页

## 相关文件

- `App.vue` - 应用启动登录逻辑
- `pages/login/login.vue` - 登录页面
- `pages/register/register.vue` - 注册页面
- `pages/add/add.vue` - 发帖页面（登录检查）
- `pages/profile/profile.vue` - 个人主页（登录检查）
- `custom-tab-bar-mp/index.js` - 小程序TabBar（登录检查）
- `custom-tab-bar/index.vue` - H5/APP TabBar（登录检查）

## 注意事项

1. **一致性原则**：所有设置用户信息的地方都必须同时设置 `isLoggedIn` 标记
2. **检查时机**：在需要登录的操作前检查 `isLoggedIn` 而非 `userInfo`
3. **清除时机**：退出登录时必须清除 `isLoggedIn` 标记
4. **调试方法**：在控制台输出 `getApp().globalData.isLoggedIn` 验证状态
