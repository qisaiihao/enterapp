# 小程序汇文明朝字体配置说明

## 已完成的配置

### 1. 字体配置更新 (`utils/fontManager.js`)

已在 `FONT_CONFIG` 中为汇文明朝字体添加小程序专用的 WOFF2 链接：

```javascript
'汇文明朝': {
    displayName: '汇文明朝',
    filename: 'Huiwen-mincho.otf',
    cloudPath: 'cloud://cloud1-5gb0pbyl400845f5.636c-cloud1-5gb0pbyl400845f5-1378788263/fonts/Huiwen-mincho.otf',
    // 小程序端使用 WOFF2 格式的 HTTPS 链接
    mpWeixinUrl: 'https://636c-cloud1-5gb0pbyl400845f5-1378788263.tcb.qcloud.la/fonts/Huiwen-mincho-compressed.woff2',
    size: 15400,
    version: '1.0.0',
    isDefault: platformDetector.getCurrentPlatform() !== 'mp-weixin',
}
```

### 2. 下载逻辑优化

修改了 `_performDownload` 方法，小程序端直接使用 HTTPS 链接，无需通过云存储获取临时链接：

- **小程序端**：直接使用 `mpWeixinUrl`（WOFF2 格式）
- **H5/App 端**：继续使用云存储路径获取临时链接

### 3. 字体加载方式

- **小程序端**：使用 `wx.loadFontFace` API 加载 HTTPS 链接的 WOFF2 字体
- **H5 端**：使用 `FontFace` API 或 `@font-face` CSS
- **App 端**：使用本地字体文件

## 工作原理

1. **App.vue 启动时预加载**：
   - 在 `onLaunch` 生命周期中调用 `fontManager.ensureFontAvailable('汇文明朝')`
   - 小程序端会自动使用 WOFF2 链接加载字体

2. **字体使用**：
   - 在 CSS 中使用 `font-family: '汇文明朝'`
   - 字体加载完成后自动应用到页面

## 兼容性说明

根据文档 `小程序字体.md`：

- ✅ WOFF2 格式支持
- ⚠️ 低版本 iOS 可能不兼容（建议使用 WOFF 或 TTF）
- ✅ 字体链接必须是 HTTPS
- ✅ 需要设置 CORS（腾讯云存储默认支持）

## 测试步骤

1. 运行编译脚本：`运行小程序构建脚本.bat`
2. 在微信开发者工具中打开编译后的小程序
3. 查看控制台日志，确认字体加载成功：
   ```
   【FontManager】小程序端直接使用 HTTPS 链接: https://...
   【FontManager】✅ 字体加载成功: 汇文明朝
   ```
4. 检查页面中使用汇文明朝字体的文本是否正确显示

## 注意事项

- 小程序端不支持加载本地字体文件，必须使用 HTTPS 链接
- WOFF2 格式文件更小，加载更快
- 字体会在 App 启动时预加载，首次加载可能需要几秒钟
- 字体加载完成前会使用系统默认字体
