# 汇文明朝字体显示修复说明

## 问题描述
修改小程序端后，H5 与 App 端的汇文明朝字体不能正确显示，但在详情页点击生成卡片之后又突然能正常显示。

## 问题原因
1. **小程序端修改影响**：在 `fontManager.js` 中为小程序端添加了跳过字体加载的逻辑
2. **条件编译问题**：在 `shareCanvas.js` 中的条件编译代码缺少日志输出，导致 H5/App 端也可能受到影响
3. **字体预加载不完整**：`main.js` 中的字体预加载逻辑不够完善，没有强制加载字体
4. **字体名称不统一**：部分代码仍使用旧的字体名称 `Huiwen-mincho`

## 修复内容

### 1. 修复 `utils/shareCanvas.js`
**位置**：第 219-231 行

**修改前**：
```javascript
// 【关键修复】小程序端使用系统字体
let actualFontFamily;
// #ifdef MP-WEIXIN
actualFontFamily = 'sans-serif';  // 小程序端强制使用系统字体
console.log('【shareCanvas】小程序端使用系统字体');
// #endif
// #ifndef MP-WEIXIN
actualFontFamily = fontFamily === 'system' ? 'sans-serif' : fontDisplayName + ', sans-serif';
// #endif
```

**修改后**：
```javascript
// 【关键修复】根据平台选择字体
let actualFontFamily;
// #ifdef MP-WEIXIN
actualFontFamily = 'sans-serif';  // 小程序端强制使用系统字体
console.log('【shareCanvas】小程序端使用系统字体');
// #endif
// #ifndef MP-WEIXIN
// H5/App端使用实际字体名称
actualFontFamily = fontFamily === 'system' ? 'sans-serif' : fontDisplayName + ', sans-serif';
console.log('【shareCanvas】H5/App端使用字体:', actualFontFamily);
// #endif
```

**说明**：添加了 H5/App 端的日志输出，明确区分不同平台的字体处理逻辑。

### 2. 增强 `main.js` 字体预加载
**位置**：第 7-60 行

**修改内容**：
- H5 端：添加 FontFace API 强制加载字体
- App 端：使用 `uni.loadFontFace` 预加载字体
- 小程序端：明确跳过字体预加载
- 统一使用字体名称 '汇文明朝'（而非 'Huiwen-mincho'）

**关键代码**：
```javascript
// H5 端
if (typeof FontFace !== 'undefined') {
  const font = new FontFace('汇文明朝', 'url(/static/fonts/Huiwen-mincho.otf)');
  font.load().then(function(loadedFont) {
    document.fonts.add(loadedFont);
    console.log('✅ [字体预加载] H5端汇文明朝字体加载成功');
  }).catch(function(error) {
    console.error('❌ [字体预加载] H5端汇文明朝字体加载失败:', error);
  });
}

// App 端
uni.loadFontFace({
  family: '汇文明朝',
  source: `url("${fontPath}")`,
  global: true,
  success: function() {
    console.log('✅ [字体预加载] App端汇文明朝字体加载成功');
  },
  fail: function(err) {
    console.error('❌ [字体预加载] App端汇文明朝字体加载失败:', err);
  }
});
```

### 3. 统一字体名称
**修改文件**：
- `utils/canvasText.js`：将 `Huiwen-mincho` 改为 `汇文明朝`
- `App.vue`：将 `@font-face` 中的 `font-family: 'Huiwen-mincho'` 改为 `font-family: '汇文明朝'`

**说明**：统一使用中文字体名称 '汇文明朝'，避免混淆。

## 为什么生成卡片后字体能正常显示？

在 `pages/post-detail/post-detail.vue` 的 `loadFontAndDraw` 方法中：

```javascript
await this.fontManager.ensureFontAvailable(fontFamily, (progress, loaded, total) => {
  console.log(`【post-detail】字体下载进度: ${progress}% (${loaded}/${total})`);
});
```

这个方法会：
1. 检查字体是否已缓存
2. 如果未缓存，从云端下载字体
3. 使用 `uni.loadFontFace` 加载字体到内存
4. 标记字体为已加载

所以点击生成卡片后，字体被强制加载到内存，之后整个应用都能正常显示该字体。

## 修复效果

修复后，H5 和 App 端将在应用启动时就加载汇文明朝字体，无需等待生成卡片操作：

- **H5 端**：通过 `@font-face` + `FontFace API` 双重保障
- **App 端**：通过 `@font-face` + `uni.loadFontFace` 双重保障
- **小程序端**：继续使用系统字体（不受影响）

## 测试建议

1. **清除缓存测试**：
   - H5：清除浏览器缓存后刷新
   - App：卸载重装应用
   - 小程序：删除小程序后重新打开

2. **验证点**：
   - 首页诗歌内容是否显示汇文明朝字体
   - 详情页诗歌内容是否显示汇文明朝字体
   - 生成分享卡片是否正常

3. **日志检查**：
   - 查看控制台是否有 "✅ [字体预加载] 汇文明朝字体加载成功" 日志
   - 查看是否有字体加载失败的错误日志

## 注意事项

1. **字体文件路径**：确保 `/static/fonts/Huiwen-mincho.otf` 文件存在
2. **条件编译**：不同平台的字体加载策略不同，注意条件编译标记
3. **兼容性**：保留了 `LEGACY_FONT_MAP` 映射，兼容旧代码中使用 'Huiwen-mincho' 的情况
